'use strict'

var Localidad = require('../models/localidad');


/*function guardarLocalidad(req,res){
  var params = req.body;
  var localidad = new Localidad();
  res.status(200).send({message: 'guarda Localidad'});
}

function getLocalidades(req,res){

  Localidad.find().exec((err, localidades) =>{
		if(err) return res.status(500).send({message: 'Error en la petición'});
		if(!localidad) return res.status(404).send({message: 'No existen localidades'});
    return res.status(200).send({localidades});
    //return localidad;
});


module.exports = {
  //guardarLocalidad,
  getLocalidades
}*/
